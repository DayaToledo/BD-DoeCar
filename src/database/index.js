const createDoacao = require('./creates/createDoacao')
const createDoador = require('./creates/createDoador')
const createInstituicao = require('./creates/createInstituicao')
const createVeiculo = require('./creates/createVeiculo')
const createVoluntario = require('./creates/createVoluntario')

module.exports = {
  createDoacao,
  createDoador,
  createInstituicao,
  createVeiculo,
  createVoluntario
}